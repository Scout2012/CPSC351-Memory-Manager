### OS Virtual Memory Manager (VMM)

The following is required to compile and run this code:
 - gcc

Helper scripts are included to either only build the program:
 - build.sh

or build and run the program:

 - build_and_run.sh
